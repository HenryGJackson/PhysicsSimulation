#include <vector>
#include <iostream>
#include <cstdlib>

namespace Integration{
    //Get the integral of the array passed in using trapezium rule
    double Integral1D(std::vector<double> values, double step_size) {
        try{ if(!array) throw 1; }
        catch(int e){
            std::cout << "ERROR: Null array passed to Integral1D()\n";
            exit();
        }
        int length = array.size();
        int i;
        for(i = 0; i < length; i ++){



        }
    }
}
